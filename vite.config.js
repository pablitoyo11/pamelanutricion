import { defineConfig } from 'vite'

export default defineConfig({
  base: '/pamelanutricion/' // nombre del repo hosteado en github pages queda web.etc"/pamelanutricion"  - si tuvieramos un host propio hay que eliminar esto porque la base seria "/"
})
